# 页面性能与视觉一致性修复说明

## 本轮定位到的问题

1. **所有按钮都启用了毛玻璃。** 全局 `button` 规则包含 `backdrop-filter: blur(18px)`，导航按钮、游戏标签、工具按钮等都会单独创建昂贵的合成层。基准页面共 91 个元素，其中 21 个元素启用了 `backdrop-filter`。
2. **背景被重复模糊。** 工作区背景图已经使用大范围 `filter: blur(28px)`，其上遮罩又使用一次 `backdrop-filter`，同一片像素被重复处理。
3. **游戏切换存在重叠的大面积动画。** 页面过渡、整个 Launcher 的缩放动画、主视觉图片的缩放动画会同时执行，既容易掉帧，也与设置页、账号页的克制动效不一致。
4. **动画参数缺少统一规则。** 不同组件使用不同的持续时间、缓动、位移距离和圆角，部分悬停还会产生突兀的缩放或位移。
5. **无下载任务时仍每秒更新时钟。** `LauncherView` 无条件运行 1 秒定时器，引发不必要的响应式更新；页面隐藏或被 KeepAlive 停用后也没有主动降载。
6. **下载轮询在后台仍保持前台频率。** 页面隐藏或 Launcher 视图停用时，轮询仍每 750ms 执行。
7. **模板内重复计算资源信息。** 游戏栏和游戏目录在一次渲染中多次计算相同标题、图标、背景图、安装状态和活动状态。
8. **稳定图片引用没有被充分利用。** 同一运行期间相同 URL 会重复执行协议转换；图片切换后也没有保留少量已解码图片，容易在重新进入页面时再次分配和解码。
9. **部分图片没有固有尺寸。** 品牌图、新闻封面、游戏图标和目录横幅缺少 `width`/`height`，图片解码前更容易产生布局调整。

## 已实施的修复

- 将毛玻璃限制在侧边栏、新闻卡片、目录遮罩和模态遮罩等少数大型层级；普通按钮、嵌套面板和工具提示改为不透明或半透明平面材质。
- 将工作区背景模糊从 28px 收敛到 18px，缩小超出视口的模糊区域，并移除遮罩层的二次模糊。
- 建立统一的动效变量：120ms / 170ms / 220ms，并统一为同一缓动曲线、控件圆角和浮层材质。
- 移除整个 Launcher 的缩放动画；主视觉只执行 220ms 淡入，页面切换只移动 4px，避免全屏重绘和“页面弹一下”的感觉。
- 完整支持 `prefers-reduced-motion`。
- 时钟只在“当前视图可见且存在下载任务”时运行；视图停用、页面隐藏或任务结束后立即停止。
- 下载轮询在前台保持 750ms，在后台或视图停用时降至 1800ms。
- 将游戏栏和目录的标题、图标、横幅、安装状态、选中状态统一预计算，避免模板重复调用。
- 为 `resourceUrl()` 加入运行期缓存；加入最多保留 24 张图片的轻量预加载/预解码缓存，并优先预热当前视觉和少量游戏目录图片。
- 为关键图片补充固有尺寸，减少解码前后的布局变化。

## 验证结果

- Vue 单文件组件解析及模板编译通过：11 个组件。
- 修改过的 JavaScript 文件通过 Node 语法检查。
- 资源 URL 缓存、图片去重、`limit: 0` 边界行为的独立断言通过。
- 使用完整 mock 数据渲染 Launcher、账号管理和设置三个页面，未出现页面错误或控制台异常。
- Launcher 实测 `backdrop-filter` 元素由 **21 个降至 2 个**。
- 同时运行的入场动画由“整个页面缩放 + 主视觉缩放”两项降为“主视觉透明度淡入”一项。

## 构建环境说明

上传包中的 `node_modules` 是 macOS arm64 版本，而当前校验环境是 Linux x64，因此 Vitest/Vite 无法加载 Rollup 的 Linux 原生可选模块。为保证交付物可验证，本项目增加了 `npm run build:standalone`：它直接使用 Vue SFC 编译器生成自包含的 `dist/index.html`，不依赖 Rollup 原生模块。本轮已完成 JavaScript 语法检查、11 个 Vue 组件解析/模板编译、单文件构建和 Chromium 浏览器烟雾测试；完整 mock 后端下 Launcher、账号管理、云同步、应用设置、游戏安装和游戏设置均无控制台错误。目标开发机重新执行 `npm ci` 后仍可照常运行原有 `npm test` 与 `npm run build`。

## Starward 设计系统对齐

本轮进一步统一了 8px 主圆角、4px 导航选中项、32/36/40/44px 控件高度、8/12/16/24px 间距、83/167/250ms 动效，以及安装和游戏设置对话框结构。详细规范见 `STARWARD_DESIGN_SYSTEM.md`。
